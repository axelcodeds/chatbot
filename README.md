# Chatbot (Ollama)

A minimal one-prompt chatbot using Ollama.

## Run

```bash
pip install -r requirements.txt
python main.py
```

Make sure Ollama is running locally and that `llama3.2:latest` is available.

